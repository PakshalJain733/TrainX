import express from 'express';
const router = express.Router();

// Placeholder for coding assessment routes
router.get('/', (req, res) => {
  res.json({ success: true, message: 'Coding routes placeholder' });
});

export default router;
